default:
  autoload:
    - "%paths.base%/tests/features/bootstrap"
  suites:
    demand_features:
      paths:    [ '%paths.base%/tests/features/demands' ]
      contexts: [ DemandContext ]
    user_features:
      paths:    [ '%paths.base%/tests/features/users' ]
      contexts: [ UserContext ]